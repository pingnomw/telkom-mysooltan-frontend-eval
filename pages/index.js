import Head from 'next/head'
import Image from 'next/image'
import styles from '../styles/Home.module.css'
import { useState, useEffect } from "react"

export default function Home() {

	const [username, setUsername] = useState("")
	const [password, setPassword] = useState("")

	return (
		<div className={styles.container}>
			<Head>
				<title>Create Next App</title>
				<meta name="description" content="Generated by create next app" />
				<link rel="icon" href="/favicon.ico" />
			</Head>

			<header>
				<h1>Login</h1>
			</header>

			<main className={styles.main}>
				<div>
					<input type="text" placeholder="GitHub username" value={username} onChange={setUsername} />
					<input type="password" placeholder="Personal access token" value={password} onChange={setPassword} />
					GitHub now requires API authentications to be done using personal access tokens instead of passwords.
					{" "}
					<a href="https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/creating-a-personal-access-token#creating-a-token" target="_blank">
						Click here to see how to get your personal access token.
					</a>
					Scopes required:
					<ul>
						<li>public_repo</li>
						<li>read_user</li>
					</ul>
					<mark>Remember to treat your personal access tokens like passwords (i.e. keep them secret)!</mark>
					<button>Login</button>
				</div>
			</main>

			<footer className={styles.footer}>
				
			</footer>
		</div>
	)
}
